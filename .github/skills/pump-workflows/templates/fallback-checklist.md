# Fallback Checklist

- Cosmos connectivity check runs at startup
- Automatic switch to in-memory mode works
- Health endpoint exposes active storage mode
- Fallback mode banner/state visible in frontend
- Data behavior in fallback (non-durable) documented
